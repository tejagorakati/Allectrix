{
  "name": "arogya-card-system",
  "version": "1.0.0",
  "description": "Digital Health Card Management System with Blockchain Logging",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && nodemon index.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && node index.js"
  },
  "keywords": ["health", "blockchain", "medical-records", "qr-code", "biometric"],
  "author": "Arogya Card Development Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}